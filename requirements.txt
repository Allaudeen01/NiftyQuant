# Core analytics
numpy>=1.26
pandas>=2.1

# Testing
pytest>=8.0

# --- Broker integration (Phase 2) ---
# The provider imports growwapi lazily, so it is only required to actually
# hit the live API / authenticate. Unit tests run without it.
growwapi>=0.1
pyotp>=2.9          # TOTP auth flow

# Angel One SmartAPI (historical data + trading). Imported lazily; only
# needed to hit the live API. Unit tests run without it.
smartapi-python>=1.3
pytz>=2024.1
logzero>=1.7            # undeclared smartapi-python dependency
websocket-client>=1.7   # undeclared smartapi-python dependency

# --- Research / experiments (Exp 003-023) ---
# Statistical models, EVT, regime and data-snooping tooling. Imported by the
# scripts/ experiments; unit tests for the pure primitives run against these.
scipy>=1.11            # stats, GPD fit, bootstrap p-values
statsmodels>=0.14      # HAC/OLS, ADF, Ljung-Box, HAR regressions
scikit-learn>=1.3      # utilities used by regime/feature work
arch>=7.0              # GARCH / EGARCH (Exp 013)
hmmlearn>=0.3          # Gaussian HMM regimes (Exp 017)
yfinance>=0.2          # overnight global data for spillover (Exp 018)
lightgbm>=4.7           # probability models A/B/C (Exp 033)

# --- Optional / later phases (kept commented until needed) ---
# scipy>=1.11            # alternative stats / optimisation
# fastapi>=0.110         # Phase: API layer
# uvicorn>=0.29
# SQLAlchemy>=2.0        # Phase: persistence
# psycopg2-binary>=2.9   # PostgreSQL/TimescaleDB driver
# redis>=5.0
# requests>=2.31         # data providers (NSE / broker REST)
